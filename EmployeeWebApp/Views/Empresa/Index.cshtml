@model IEnumerable<EmployeeWebApp.Models.Empresa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomeDaEmpresa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Endereco)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomeDaEmpresa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Endereco)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EmpresaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EmpresaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EmpresaID })
        </td>
    </tr>
}

</table>
